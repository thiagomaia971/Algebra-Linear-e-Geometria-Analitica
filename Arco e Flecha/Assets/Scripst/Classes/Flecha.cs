using UnityEngine;
using System.Collections;

public class Flecha : MonoBehaviour {
    /*private int velocidadeArrow = 3;
    public GameObject pontoAArrow;
    public GameObject pontoBArrow;
    public Vector3 a;
    public Vector3 b;

    static private Ponto pontoAPlano = new Ponto(15, 3.5, 1.5);
    static private Ponto pontoBPlano = new Ponto(15, 1, -1.5);
    static private Ponto pontoCPlano = new Ponto(15, 3.5, -1.5);

    static private Reta AB = new Reta(pontoAPlano, pontoBPlano);
    static private Reta AC = new Reta(pontoAPlano, pontoCPlano);

    static private Plano plano = new Plano();

    private Reta N = plano.CalcularN(AB, AC);
    

    static private Ponto pontoA;
    static private Ponto pontoB;
    static private Reta retaABArrow;


    void Start () {
        
        
        print("AB("+ AB.x +", " + AB.y +", "+ AB.z +")");
        print("AC(" + AC.x + ", " + AC.y + ", " + AC.z + ")");
        print("N(" + N.x + ", " + N.y + ", " + N.z + ")");
       // print( N.ProdutoEscalar(N, A));
    }
	
	// Update is called once per frame
	void Update () {
        pontoA = new Ponto(pontoAArrow.transform.position.x, pontoAArrow.transform.position.y, pontoAArrow.transform.position.z);
        pontoB = new Ponto(pontoBArrow.transform.position.x, pontoBArrow.transform.position.y, pontoBArrow.transform.position.z);
        retaABArrow = distAB(pontoA, pontoB);
        print(retaABArrow.x);

    }
    private Reta distAB(Ponto A, Ponto B) {
        Reta AB = new Reta(A, B);
        return AB;
    }*/
}
